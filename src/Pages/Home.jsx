import React from 'react';
import Hero from '../components/Hero/Hero';

const Home = () => {
    return (
        <section className='bg-primary'>
            <Hero />
        </section>
    );
};

export default Home;